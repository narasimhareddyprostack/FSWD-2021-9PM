let size = [1, 2, 3, 4]; //size is array or we called iterable object.

//display all values.
//1. forEach

/* size.forEach((x) => {
  console.log(x);
});
 */
size.forEach((x) => console.log(x)); // implicit return

//2.Map
size.map((x) => console.log(x)); // implicit return
//3.filter
size.filter((x) => console.log(x)); // implicit return

//when we have use?


const words = [
  "spray",
  "limit",
  "elite",
  "exuberant",
  "destruction",
  "present",
];

//assignment -1 for

let newWords = words.filter((x) => x.length >= 6);

console.log(newWords);

// filter - expecting call back fun/arrow function , iterates array values
// retunr new array based on condition

class Address {
  constructor(hno, area, city, state) {
    this.houseNo = hno;
    this.area = area;
    this.city = city;
    this.state = state;
  }
}

//export default Address; // ES6 format 
//module.exports = Address;  //ES5 format
